# Phase 5 — UI/UX polish only.
# Native Streamlit theming for the dark, charcoal + orange cinematic look.
# This is purely visual (colors/fonts) — no backend, data, or routing
# behavior lives here.

[theme]
base = "dark"
primaryColor = "#2979FF"
backgroundColor = "#121212"
secondaryBackgroundColor = "#1A1A1A"
textColor = "#F5F5F3"
font = "sans serif"
